const assertString = require('./util/assertString');
const { fullWidth } = require('./isFullWidth');
const { halfWidth } = require('./isHalfWidth');
export default function isVariableWidth(str) {
  assertString(str);
  return fullWidth.test(str) && halfWidth.test(str);
}